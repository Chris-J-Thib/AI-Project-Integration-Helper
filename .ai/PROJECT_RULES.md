# Project Rules

Project-specific restrictions for this codebase, in addition to the
rules in `.ai/CONTRACT.md`. Empty by default.

Only add rules here when the user explicitly confirms it - see
`.ai/CONTRACT.md`'s "Core Rules" for the exact process.

<!-- e.g.
- Never edit files under /legacy - it is frozen for reference only.
- Always run the test suite before marking a task done.
- This project is not git-enabled; do not attempt git commands.
-->
